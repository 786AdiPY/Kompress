# MLflow tracking server backed by PostgreSQL (metadata) + MinIO/S3 (artifacts).
# The command (mlflow server ...) is supplied by docker-compose.
FROM python:3.12-slim

RUN pip install --no-cache-dir "mlflow>=2.13" psycopg2-binary boto3

EXPOSE 5000
CMD ["mlflow", "--version"]
