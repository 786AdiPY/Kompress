# ── Core pipeline (always needed) ────────────────────────────────────────────
scikit-learn>=1.4.2
pandas>=2.0.0
numpy>=1.26.4
pyyaml>=6.0
mlflow>=2.13.0

# ── ONNX export + quantization + runtime (universal compression target) ──────
onnx>=1.16.0
onnxruntime>=1.18.0        # provides onnxruntime.quantization (CPU INT8)
onnxmltools>=1.12.0        # XGBoost / LightGBM -> ONNX
skl2onnx>=1.16.0           # scikit-learn -> ONNX

# ── Serving ──────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.1

# ── Deploy (Hugging Face Spaces) ─────────────────────────────────────────────
huggingface_hub>=0.23.0

# ── Framework extras (install the ones you use) ──────────────────────────────
xgboost>=2.0.3            # classic-ML baseline trainer + adapter
# lightgbm>=4.0.0         # uncomment for LightGBM models
# torch>=2.2.0            # uncomment for PyTorch models (large download)

# ── GPU-only (optional; pipeline falls back to ONNX INT8 without these) ──────
# tensorrt                # requires NVIDIA GPU + CUDA
# pycuda                  # requires NVIDIA GPU + CUDA
